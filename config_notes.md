## face_api
- admin/facerecofe/src/layouts/database/data/tableFields.tsx > line 162: change URL to baseURL of rekoWrap

# to run:
- cd to `admin/rekoWrap` and run `pip install -r requirements.txt`
- run `python app.py`
- open a new shell
- cd to `.` and run `docker-compose up --build`